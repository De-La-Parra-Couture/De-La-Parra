// =============================================================================
// De la Parra Couture — Animations Index
// =============================================================================

import { initHeader } from './header';
import { initHero } from './hero';
import { initReveal } from './reveal';

export function initAnimations(): void {
  initHeader();
  initHero();
  initReveal();
}
